import { ListItem, styled } from "@mui/material";

export const LinkItemSidenav = styled(ListItem)(({ theme }) => ({
  width: "70%",
  textAlign: "center",
  borderRadius: "5px",
  margin: "0 0px 20px 0px",
})) as typeof ListItem;
